---
users:
  - name: anthony
    groups: wheel
    state: present
    shell: /bin/bash
    key: "{{ anthony_key }}"

  - name: awheeler
    groups: users
    state: present
    shell: /bin/rbash
    key: "{{ awheeler_key }}"

  - name: ljennison
    groups: users
    state: present
    shell: /bin/rbash
    key: "{{ ljennison_key }}"

  - name: rob
    groups: infosec
    state: present
    shell: /bin/rbash
    key: "{{ rob_key }}"

  - name: sdoran
    groups: wheel
    state: present
    shell: /usr/bin/fish
    key: "{{ sdoran_key }}"

  - name: tbadawy
    groups: users
    state: present
    shell: /bin/rbash
    key: "{{ tbadawy_key }}"
